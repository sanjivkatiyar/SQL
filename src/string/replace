select *, replace (country, 'United States', 'US') as new_country from customer;

select *, replace (country, 'United States', 'US') from customer;

select *, replace (lower(country), lower('United States'), 'US') from customer;


select * from customer;

select cast(replace(age,'0', '') as int) from customer;